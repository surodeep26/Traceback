This example reads the output of traceback and finds the line with the
'best' case, the criterion for which must be defined in the source file.

The usage is

	bestcase <output file>

It doesn't use a configurationn file.
